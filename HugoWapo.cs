using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HugoWapo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("coco seco 83");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
